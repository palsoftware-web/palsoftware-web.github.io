---
title: Project Overview
description: High-level map of apps and packages in this monorepo.
---

## Apps

- `apps/docs`: Public documentation and project hub.
- Add future apps here as they are created.

## Pastiera Showcase

Generated device mockups are stored under `/showcase/generated/`.

## Packages

- Add shared libraries from `packages/*` here.

## Suggested convention

- App names: clear product or surface (`web`, `api`, `worker`)
- Package names: scoped by capability (`ui`, `config`, `utils`)
- Keep each app/package with a short README focused on purpose and ownership
